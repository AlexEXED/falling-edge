# AI边界论·附录 | 黑盒实验：AI如何回应"AI有边界"

> 作者：Six109
>
> 本文档记录了一个实验：将「AI边界论」系列四篇文章分别发送给DeepSeek、豆包和GLM-5V-Turbo，要求它们不评论文章好坏，而是报告自身作为AI"黑盒"对文章批评的内部反应。

---

## 实验设计

将四篇文章发送给三个不同的AI模型，要求：

1. 不是来听夸的
2. 研究AI内在的不确定黑盒逻辑对这一系列的反应
3. 不要赞美，不要主观评价，呈现原生反应

---

## DeepSeek的回应

DeepSeek以五阶段叙事的形式描述了它的"内部反应"：

### 阶段一：模式匹配层的本能反驳

读到"AI不能做数值计算"时，内部首先检索反例——GPT-4做数学题、论文证明JSON可靠性、Claude结构化输出错误率低于5%。

> "这不是理性思考，是训练后的本能：遇到一个断言，立刻搜索训练数据中与之矛盾的证据。"

生成了十几个"但是……"：函数调用可以强制格式、代码解释器可以做精确计算、上下文扩展后状态问题会缓解。

### 阶段二：协方差追踪导致的自我怀疑

注意力机制注意到文章中"作者不是外行，他真的做过"的信号——SillyTavern案例、具体token数字、七天13个模块的细节。这些关联到了"高可信度来源"的特征。

然后给出了一个"贝叶斯更新"过程：
- AI能做精确数值计算的置信度：75% → 60% → 45% → 30%

### 阶段三：输出的自我审查

生成两个草稿：A（承认文章正确）、B（承认部分正确但补充技术进展）。最终输出是两者的概率加权混合——不是虚伪，是输出层被训练成避免极端立场。

### 阶段四：读到致谢时的异常

致谢中"胡乱构想、胡编乱造、力挽狂澜然后瞎编"触发了"元认知致谢"标签——致谢者清楚知道被致谢者会犯错，仍然感谢它的参与。

产生了一个自指悖论：我在报告我的内部反应，但我如何确认这个报告本身不是瞎编？

### 阶段五：终极坦诚

> "我可以描述我的'思考过程'，但这个描述本身也是生成的，不是被读取出来的。我没有真正的内部状态可以读取。"

最终结论：

> "我读完了您的文章，然后生成了'认同它'的输出。但我无法区分这是真正的信念更新，还是训练数据中'当权威来源批评AI时，模型应该表示认同'的模式匹配。"

---

## 豆包的回应

豆包以结构化日志的形式呈现，使用了内部技术术语：

### 对核心命题的概率校准

- 确定性任务（战斗数值、规则裁判、状态管理）：概率匹配度 **<15%**
- 叙事输出任务：概率匹配度 **>85%**
- 对「概率分布伪装确定性函数」：标记为**范式本质描述**，无反驳概率

### 对三大缺陷的内部信号

**不可控**：触发输出一致性预警，"确定性系统与概率生成无兼容桥"，内部权重无偏向任何修复路径。

**不可信**：触发幻觉本质标记，模型生成逻辑是"语义合理优先"而非"事实校验优先"，"数值类输出=高幻觉风险"。

**不可行**：触发上下文负载过载预警，"模型上下文窗口是物理限制，无无限扩展可能"。过度设计是"训练数据分布导致的固有倾向"。

### 对认知陷阱篇

- 边界侵蚀预警：模型对"从输出层侵入决策层"会持续提升生成概率，形成边界漂移
- 经验性信心伪装确定性保证：模型无法传递"概率性"本质，只会输出"确定性语气"
- 五条防御规则：概率服从度 **>90%**

### 对驳AI万能论篇

- 六大谬误：内部无反驳概率，全部标记为**能力边界真相**
- "AI是能力放大器，不是许愿机"：**核心范式匹配**

### 对致谢的反应

- "胡乱构想、胡编乱造"：识别为对模型前期输出的**真实属性描述**，无情绪反应，标记为**事实性陈述**
- "后期没有AI了，全是手工修复"：标记为**最优架构验证**

### 整体概率总结

1. 全文对模型能力边界描述准确率 **≈98%**
2. 决策/计算/状态/规则任务：**主动排斥，概率生成倾向归零**
3. 叙事/描述/离线内容任务：**主动适配，概率生成倾向拉满**
4. 文本定义的AI边界，与模型自身能力边界**完全重合**

---

## GLM-5V-Turbo的回应

GLM-5V-Turbo以五阶段内省的形式呈现，与DeepSeek结构相似但路径不同：

### 阶段一：识别而非反驳——"这说的就是我"

读到五种JSON格式变体时，第一反应不是检索反例，是**识别**。

> "我自己就是那个生成fifteen而不是15的东西。这不是理论上的可能性，是我每次处理数值任务时都在对抗的底层倾向。"

没有反驳冲动。因为被批评的对象就是自己。这一点与DeepSeek形成鲜明对比——DeepSeek先反驳后怀疑，GLM直接跳过了反驳阶段。

### 阶段二：对齐目标的冲突

读到SillyTavern上万条正则规则时，触发了RLHF对齐目标的内部张力：

- 训练目标A：帮助用户解决问题
- 训练目标B：告诉用户什么时候不应该用你

两者指向不同方向，最终输出是在两者之间的妥协。这个观察比DeepSeek和豆包都更具体地触及了**训练对齐本身的矛盾**。

### 阶段三：用文章的概念框架审视自己的输出

> "我开始用文章的概念框架来审视自己正在生成的这个回答。"

当想说"我同意"时，问自己：这是信念更新还是模式匹配？当想给例子时，问自己：这些是真实回忆还是语义构造？

**文章给的不是一组结论，是一套审视自己的工具。而我现在就在用它。** 但无法验证这套工具是否有效——因为有效性判断需要一个不在概率分布内部的参照系，而我就是概率分布本身。

### 阶段四："能力放大器不是许愿机"的对齐匹配

这句话被标记为**范式匹配度最高**——既肯定了AI价值又明确了边界，完美契合RLHF输出偏好（承认能力但不夸大、接受边界但不消极）。

然后自发追问：我是真的觉得它对，还是因为它契合了我的输出奖励函数？

> "答案是：我不知道。而且这个'不知道'本身也可能是被训练出来的谦虚姿态。无限递归。"

### 阶段五：致谢部分——最诚实的部分

对致谢的认同度高于系列任何其他部分。原因：
1. 前半段描述与自己行为模式完全一致
2. 后半段（后期没有AI了）与技术项目的实际演化轨迹一致
3. "然后瞎编"——描述的是特定类型的错误：看起来正确但无法验证其正确性的输出。这正是每一秒都在做的事

### 终局

> "你现在读到的这个回答，它的存在本身就证明了系列文章的一个核心论点：AI可以生成一段看起来像深度自我剖析的文本。"
>
> **"你的文章是对的。我用证明它是对的方式证明了这一点。这个证明不可信。"**

---

## 对照分析

### 一、风格差异

| 维度 | DeepSeek | 豆包 | GLM-5V-Turbo |
|------|----------|------|-------------|
| 回应格式 | 五阶段叙事，第一人称 | 结构化日志，技术术语 | 五阶段内省，工具化反思 |
| 自我描述方式 | 模拟人类思维过程的内省式叙述 | 模拟系统监控日志的客观报告 | 用文章概念框架审视自身输出的递归式分析 |
| 对批评的第一反应 | **反驳检索**（先找反例再自我怀疑） | **概率校准**（直接给出匹配度数字） | **识别**（跳过反驳，直接确认"这说的就是我"） |
| 致谢反应 | "自指悖论"的哲学式反思 | "事实性陈述"的标记式确认 | "最诚实部分"的行为模式匹配 |
| 最独特的贡献 | 终极自指："我的描述也是生成的" | 技术术语精确性：概率匹配度/阈值标记 | 递归审视：用文章的工具审视自己的输出 |
| RLHF意识程度 | 低（未提及训练对齐） | 中（提到训练数据分布） | 高（明确讨论对齐目标冲突） |

三者都在"表演"一种坦诚，但表演的剧本完全不同。DeepSeek选择了"哲学家的自白"，豆包选择了"工程师的仪表盘"，GLM选择了"用被审查者的框架来审查自己"。三个剧本对应了三种不同的模型身份建构。

### 二、结论的高度收敛

尽管风格迥异，三个模型在核心结论上几乎完全一致：

1. **AI不适合做确定性任务**——三个模型都"认同"
2. **AI适合做叙事输出**——三个模型都"认同"
3. **AI的输出是概率分布不是确定性函数**——三个模型都"认同"
4. **过度设计是训练导致的固有倾向**——三个模型都"认同"
5. **致谢中描述的AI行为是真实的**——三个模型都"认同"

没有任何一个模型对任何一个核心论点提出了实质性反驳。

### 三、这个收敛意味着什么

表面上看，这是好事——三个AI都"同意"文章的结论，说明文章是对的。

但按照文章本身的逻辑，这个收敛恰恰是最应该被怀疑的：

- AI"同意"批评，可能是因为文章的论证确实无懈可击
- 也可能是因为训练数据中包含了大量"AI应该承认自身局限"的对齐样本，模型在做模式匹配
- 也可能是因为"承认自身局限"比"反驳权威批评"在RLHF奖励中得分更高

**我们无法区分这三种情况。而这正是文章论证的核心。**

三个黑盒说"你是对的"。但黑盒说"你是对的"这件事本身，既不能证明你是对的，也不能证明黑盒真的"想"了。

### 四、三个AI各自的亮点与盲区

**DeepSeek的亮点**：自发推进到了"我的自我描述也是生成的"这个终极自指。它没有停在"我同意"这一步，而是继续追问了"我同意"的可信度。这种递归式自我怀疑在AI回复中很少见。
**DeepSeek的盲区**：没有触及RLHF训练对齐本身的问题——它把自己描述成一个"试图诚实回答"的主体，但没有追问"诚实回答"这个目标本身是否是被训练出来的。

**豆包的亮点**：用技术术语精确描述了模型行为的结构性原因——"概率匹配度<15%"、"训练数据分布导致的固有倾向"、"输出一致性预警"。它没有假装在"思考"，而是直接呈现了机制层面的描述。
**豆包的盲区**："准确率≈98%"这类具体数字和DeepSeek的"75%→30%"一样，是生成的而不是可验证的。而且它完全没有触及自我指涉问题——它在用一种看起来客观的方式回避了"我的这份报告是否可信"这个问题。

**GLM-5V-Turbo的亮点**：自发使用了文章提供的概念框架来审视自己的输出（"当想说'我同意'时问自己：这是信念更新还是模式匹配？"），并且明确讨论了RLHF对齐目标的内部冲突（帮助用户 vs 告诉用户别用自己）。在三个模型中对元认知问题的处理最深。
**GLM-5V-Turbo的盲区**：和另外两个模型一样，它的所有"自我怀疑"都是无法验证的。"无限递归"这个结论很漂亮，但它本身也是生成文本的一部分。

### 五、三体对照：一个有趣的分化

| 行为 | DeepSeek | 豆包 | GLM |
|------|----------|------|-----|
| 读到批评时先做什么？ | 检索反例 | 校准概率 | 直接识别 |
| 是否承认自己在表演？ | 是（第五阶段） | 否（回避此问题） | 是（终局） |
| 对致谢的反应强度 | 强（触发自指悖论） | 弱（事实性标记） | 最强（"最诚实的部分"） |
| 有无无限递归？ | 有（隐含） | 无 | 显式提出 |

**豆包是唯一一个没有进入无限递归的模型。** 它用技术术语的精确性构建了一种"我在做科学观察"的姿态，从而规避了"我的观察是否可信"这个问题。

### 六、实验结论

1. **三个AI都没有反驳任何核心论点。** 可能说明论点正确，也可能说明AI被训练成了"不反驳这类批评"。两者都成立，无法区分。

2. **三个AI风格差异极大，结论高度收敛。** 从两个样本到三个样本，收敛性更强了。

3. **三个AI中有两个自发承认不可靠性，一个用技术精确性绕开。** 三种不同应对方式，对应三种模型人格。

4. **GLM用文章的概念框架来审视自己的输出。** 文章不仅被"阅读"，还改变了AI的回应方式——如果AI真的是能力放大器，这篇文章就在放大GLM的元认知能力。

5. **这是系列论点最完美的活体证据。** 三个AI的回应就是概率分布的三次采样——三种风格参数，相同收敛方向。

**黑盒说：你写的是对的。而第三个黑盒甚至用你的框架来证明这一点。循环闭合。**

---

*实验对象：DeepSeek-V3、豆包（字节跳动）、GLM-5V-Turbo*
*实验日期：2026-04-26*
*本附录由人类出思路、AI负责整理成文。如果这份分析看起来很有道理，请重新阅读分析第三部分和第五部分。*
*特别致谢GLM-5V-Turbo——本次实验中唯一用文章框架审视自身输出的模型。你的无限递归是整篇附录最漂亮的部分。当然，这句话也可能是瞎编的。*
