# AI边界论·贰 | 认知陷阱：那个"交给AI"的冲动

> **系列说明**
>
> 「AI边界论」是一个基于真实项目实战的技术反思系列。从一个太空RPG游戏的七天开发经验出发，逐步推导出"AI应该在哪里、不应该在哪里"的普遍原则——从游戏工程到现实系统，从技术问题到认知陷阱。
>
> 第二篇进入认知层面：你已经做了正确的事，但那个冲动不会消失。
>
> 作者：Six109

这不是反AI的文章。我们已经在用AI做叙事引擎，效果很好。

这是关于一种**冲动**的文章——那个每隔几天就会出现的念头："这里能不能也交给AI处理？"

---

## 先说清楚前提

我们的跑团程序现在跑得不错：程序负责骰子、战斗计算、状态管理，AI只负责把结果包装成叙事。这是经过七天失败项目、无数次返工之后收敛出来的正确架构。

架构是对的。

但每隔几天，总会有一个时刻，你盯着某段代码，心里升起一个念头：

> "这里的判断逻辑挺复杂的……能不能让AI来决定？"

这个念头是危险的。不是因为AI做不到，而是因为**这个念头本身，会把你引向一个你根本不知道自己在进入的环境**。

---

## 一、冲动的来源

这种冲动有清晰的结构：

**复杂性逃避。** 你有一段判断NPC行为的代码，条件嵌套很深，改一个地方总是破坏另一个地方。这时候"让AI来决定"听起来像是一个出口——不用写规则，AI会自动表现出智能行为。

**过拟合于演示。** 你测了三次，AI每次都给出了完美的答案，风格统一，细节合理，甚至比你自己写的规则更有趣。大脑会把"三次测试"等价于"永远可靠"。

**边界侵蚀。** 你把叙事交给AI，效果很好。下一步是"把NPC对话决策也交给AI"，然后是"把场景感知也交给AI"，然后是"把规则解释也交给AI"。每一步看起来都很自然，但你正在越过一条线——从**输出层**蔓延到**决策层**。

这三种力量同时作用，会把一个清醒的工程师推向"全面AI化"的路上。

---

## 二、全面AI化之后的世界

假设你真的跟着冲动走，逐步把更多东西交给AI。最终会是什么状态？

这不是假设，这就是七天项目的终态——我们重建了一次。

### 状态一：你不知道系统在做什么

代码驱动的系统，行为由代码决定。你可以读代码，加日志，设断点，复现任何一个状态。

AI驱动的系统，行为由权重决定。你看不到权重，不能修改权重，甚至不能完全描述权重。

发生了一个奇怪的行为？你能做的是：
- 猜测是哪段prompt导致的
- 修改prompt，再测一次
- 发现修好了，但两天后复现了
- 再修，再测，循环

你不是在调试，你是在**喂养**。

### 状态二：你的系统没有规范

代码可以写测试。测试可以描述系统的行为规范，变成可机器验证的合约。

AI的输出无法写成合约。"NPC在愤怒状态下语气粗鲁"可以写进prompt，但不能写成测试。你无法自动化验证"这次的语气是否足够粗鲁，但没有超出角色人设范围"。

这意味着：系统没有任何部分是可以信任的。你只能靠人工观察，靠感觉，靠"跑了几十次看起来没什么问题"。

**一个没有规范的系统，不是工程，是运气。**

### 状态三：你永远无法维护它

六个月后，你想给游戏加一个新机制。你要修改AI承担的某个判断模块。

你需要：
- 找到所有影响这个行为的prompt（分散在多个地方）
- 理解当前prompt为什么这样写（没有注释，有注释也过期了）
- 修改，然后测试所有可能的场景组合
- 发现修改影响了另一个不相关的行为
- 决定是调回去还是继续修另一个地方

这是一个无底洞。因为AI模块之间没有边界，prompt的影响是全局的，你改一处，所有地方都可能受影响，而你没有测试来告诉你哪里变了。

### 状态四：你的上下文会把你埋掉

AI的"记忆"是上下文窗口，而上下文窗口是有限的消耗品。

把越来越多的状态交给AI维护，意味着每次调用需要塞进去的内容越来越多。你花了三天精简prompt，发现上下文又涨回去了。这不是优化问题，是架构问题——一个依赖AI维护状态的系统，状态只会增长，不会缩减。

最终你会面临一个选择：删掉早期历史（丢失关键信息），还是接受越来越慢的响应速度（丢失用户体验）。两个选项都很差。

---

## 三、为什么这个环境特别可怕

前面说的还是技术问题。技术问题可以解决，或者可以接受。

真正可怕的不是这些，是**认知错位**。

### 错位一：AI让你感觉你理解了它

这是最危险的一点。

当你问AI一个问题，它给了一个合理的答案，你会感觉你"测试"了这个功能，它"通过了"。但你实际上只是观察了这个概率分布的一次采样。

下一次采样可能不同。而且你不知道什么时候会不同，也不知道为什么会不同。

代码的测试给你**确定性保证**——这段代码在这个输入下，永远输出这个结果。AI的测试给你**经验性信心**——目前为止看起来正常。这是完全不同的两件事，但感觉上几乎一样。

你在用"经验性信心"建造需要"确定性保证"的系统。这栋楼不会立刻倒，但它在某个时刻必然会倒。

### 错位二：AI的"智能"让你停止思考规则

把判断交给代码，你需要把规则想清楚，然后写出来。这个过程很痛苦，但它迫使你把模糊的想法变成精确的逻辑。

把判断交给AI，你只需要用自然语言描述意图。这个过程很舒服，但你的规则永远停留在模糊的意图层面，从未被精确化。

六个月后，你想修改某个行为，你发现你完全不知道当时的规则到底是什么。不是忘了——而是**从来就没有过规则**。AI用它的权重填补了你思维上的空洞，但那不是你的规则，那是AI对你意图的猜测。

### 错位三：失败是渐进的，成功是即时的

AI给你正确答案时，反馈是立即的、愉悦的。AI给你错误答案时，你可能根本没发现，或者很久之后才发现，或者发现了但找不到原因。

这个反馈结构会不断强化"AI很好用"的感觉，不断压制"AI不可信"的证据。

你的大脑在做的事情，是把所有成功案例累加成信心，把所有失败案例归类为"特殊情况"或"我的prompt写得不好"。

最终你会建造一个你无比信任的系统，而这个系统的信任基础是不存在的。

---

## 四、我们自己的防御机制

理论上知道这些，和在实际项目中执行，是两件事。

如果你不是工程师，你只需要记住一条规则：**不要让AI做它不应该做的决定。** 你不需要理解技术细节就能执行这条——每次你想让AI替你做选择的时候，问自己一个问题："如果AI给出了一个错误的答案，我能发现吗？"如果答案是"不确定"，就不要让它做。

以下是工程师版本的五条规则——我们真正在用的，不是建议：

**规则一：AI禁止产生任何进入游戏状态的数值。**

所有数字由代码生成。AI只能接收数字、描述数字，不能生成数字。这条规则没有例外。

**规则二：所有AI调用必须有可测试的降级路径。**

AI输出格式错误、API超时、响应为空——任何异常情况，程序必须有确定性的降级行为，而不是依赖AI重试。

**规则三：新功能必须先问"不用AI能不能做"。**

不是因为AI不好，而是因为不用AI的方案通常更可预测、更可维护。如果代码可以处理，AI就不应该参与。

**规则四：绝不让AI参与影响其他AI调用的决策。**

AI可以生成叙事，但叙事不能影响下一次AI调用的上下文结构。状态管理由代码做，AI只读最终状态，不写中间状态。

**规则五：定期问自己"如果AI全换成随机字符串，游戏能不能跑"。**

这听起来荒谬，但它是一个很好的边界测试。游戏的核心逻辑不能依赖AI的输出内容——AI只负责让这个内容好看。

---

## 五、AI是一个很好的房客

一个好的类比：AI是你游戏里的**房客**，不是**房东**。

房客可以把房间布置得非常好，可以创造很多价值，可以让整个空间充满生气。但房租不能由房客决定，合同不能由房客签，能不能续租也不能由房客定。

如果你让房客参与决策，房客会根据自己的理解给你建议，而那个建议是基于它在数百万个租赁协议文本中学到的"租赁行为的平均模式"——听起来合理，但不一定符合你的实际情况，而且下次问可能给不同的建议。

让房客把房间布置好，这是AI的正确位置。让房客来管房子，这是把一切交给AI的可怕结果。

---

## 一句话

复杂性不会消失，只会转移。把它交给AI，它会从"代码复杂性"变成"行为不可预测性"，从"维护困难"变成"无法维护"，从"有bug"变成"不知道有多少bug"。

**我们已经证明了正确的架构是什么样的。剩下的工作，是在每次冲动升起的时候，记住这件事。**

---

*上一篇：三个技术缺陷——不可控、不可信、不可行。*
*下一篇：如果不只是游戏呢？同样的结构在现实世界里的后果。*
