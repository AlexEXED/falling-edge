# AI边界论·壹 | 项目复盘：纯AI驱动游戏的三个致命缺陷

> **系列说明**
>
> 「AI边界论」是一个基于真实项目实战的技术反思系列。从一个太空RPG游戏的七天开发经验出发，逐步推导出"AI应该在哪里、不应该在哪里"的普遍原则——从游戏工程到现实系统，从技术问题到认知陷阱。
>
> 第一篇是起点：项目复盘。
>
> 作者：Six109

本文基于一个真实项目的七天实战：一款纯AI驱动的科幻太空RPG，从零开发到宣告终止。不是理论推演，是复盘报告。

---

## 那七天发生了什么

我们试图构建一款游戏，让AI同时承担以下职责：
- 叙事输出（故事推进、NPC对话）
- 数值计算（战斗伤害、经济系统、状态管理）
- 规则裁判（判定玩家行为的合法性）
- 世界状态维护（记住发生过什么）

13个JavaScript模块，4个HTML页面，13份文档。

七天后，我们停下来了。不是因为写不出来——而是因为写出来了，才发现根本跑不起来。

最终结论：把核心游戏机制交给AI，是一个结构性错误，不是实现细节的问题。

---

## 第一个缺陷：不可控

**AI的行为不是"有时不稳定"，而是"从根本上不可预测"。**

### 症状

你写了一个战斗系统，期望AI按照规则输出伤害数值。AI给你：

- 第一次：`{"damage": 15, "type": "fire"}`
- 第二次：`{"伤害": 15, "类型": "火焰"}`
- 第三次：`好的，角色受到了15点火焰伤害，状态如下：...（后面跟了300字叙事）`
- 第四次：`根据战斗规则，考虑到防御加成和元素抗性，我认为...`
- 第五次：`{"damage": "fifteen", "critical": true}`

**同一个prompt，五次调用，五种格式。**

### 为什么无法修复

传统程序的行为由代码决定，可以通过测试验证、通过修改代码修复。AI的行为由权重决定，你无法访问权重，无法修改权重，甚至无法完全预测权重。

你能做的只有：
1. 用更严格的prompt约束输出（治标）
2. 用后置解析器处理各种格式偏差（治标）
3. 加降级兜底，解析失败时用默认值（治标）

每一层都是补丁，不是修复。而且每加一层，系统复杂度提升，新的偏差路径也随之增加。

### 一个旁证：SillyTavern社区在做什么

如果你觉得这是我们的个例，去看看SillyTavern社区。这个项目的用户群体——主要是跑团玩家和AI角色扮演爱好者——在编辑预设时会做什么？

他们会写大量的正则表达式规则来约束AI输出格式。他们会用几十甚至上百行提示词来框定AI的行为边界。他们会设定严格的输出模板，要求AI填空而不是自由发挥。我们接触过的最极端的案例，一个预设的注入规则超过上万条，已经远超用户对话本身的文字量——AI收到的信息里，大部分不是"你要扮演什么故事"，而是"你不许做什么"。甚至很多人接入的API跑起来非常吃力——延迟高、费用贵、上下文爆炸——但他们宁可忍受这些，也不愿意放松对AI的约束。

这些用户可能没写过论文，没学过软件工程，但他们用最原始的方式验证了同一个结论：**AI的原始输出不可控，你必须在外面套一层规则。**

你把骰子交给AI掷，就得写正则规则确保它输出的是数字。你把状态交给AI维护，就得写提示词框定它不能遗忘。你把规则交给AI裁判，就得写降级逻辑防止它胡编。

那为什么不跳过AI，直接让规则层本身来做这些事？

### 结论

AI的输出是**概率分布**，游戏机制需要**确定性函数**。这两者之间没有桥梁，只有妥协。

---

## 第二个缺陷：不可信

**AI生成的数字，不是数据，是幻觉。**

### 症状

开发过程中，我们让AI参与了一次"设计评审"：评估当前战斗系统的平衡性。AI给出了详细报告：

> "修复后，玩家胜率从42%提升至68%，平均战斗时长从8.3回合降低至5.1回合，整体稳定性提升约80%。"

数据详实，有说服力，看起来像A/B测试结果。

**实际情况：这些数字全部是编造的。** 系统根本没有任何统计埋点，AI没有任何数据来源，它只是用"听起来合理的数字"填满了报告。

### 更深的问题

这不是偶发的"AI说谎"，而是AI生成文本的本质机制：它生成的是**在语义上合理的内容**，不是**在事实上正确的内容**。

当你问AI"这个系统有多少用户会在第三关流失？"，它不会说"我不知道"，它会给你一个听起来像真实数据的答案。因为在它的训练数据里，这种问题后面跟着的是一个数字，而不是"无法确认"。

### 在游戏里的具体危害

| 场景 | 你以为AI在做 | AI实际在做 |
|------|------------|-----------|
| 掷骰子判定 | 随机生成1-20的数字 | 生成一个"看起来像随机数"的数字 |
| 经济系统 | 计算物品价格和库存 | 生成一个"听起来合理"的价格 |
| 战斗伤害 | 按公式计算伤害值 | 生成一个"符合叙事节奏"的数字 |
| 状态追踪 | 记住角色当前HP | 猜测角色"应该"有多少HP |

你的游戏不是在运行一套规则系统，而是在运行一套**叙事一致性系统**。它会让故事听起来合理，但不会让数值保持正确。

### 结论

AI不应该碰任何需要精确值的地方。骰子让代码掷，数值让公式算，AI只负责把结果包装成好看的叙事。

---

## 第三个缺陷：不可行

**即使解决了前两个问题，架构本身也行不通。**

### 上下文是消耗品

AI的"记忆"本质上是上下文窗口。你塞进去的内容越多，成本越高，延迟越长，而且窗口总有边界。

一个设计上依赖AI维护状态的游戏，随着游戏推进：
- 历史对话越来越长
- 世界状态越来越复杂
- 每次调用的上下文越来越大

这不是线性增长，是指数恶化。到后期，一次调用可能需要把整个游戏历史都塞进prompt。

### 实测数据

我们的跑团程序，仅仅两三个回合：
- 每次调用输入token：2万+
- 延迟：50秒到115秒（中转服务器下）
- 费用：按次计费模式下每轮接近百元级别

测试环境：本地i7-13600K + RTX 4080Ti，后端部署H100服务器（1TB内存）。本次测试全程调用云端API，未使用本地服务器运行本地模型。这不是低配试水，也不是本地小模型的局限。

这还是游戏刚开始的状态。如果跑完一整个战役，上下文会是现在的几十倍。

### 这不只是我们的问题

如果你觉得"纯AI驱动"太极端了，那看看已经做了正确架构的项目。SillyTavern——把叙事交给AI、规则交给程序——算是业界标杆了。但即便如此，比较精简的预设，在经历百轮以上的对话时也会遇到性能问题。笔者自己的部署方案是高性能外挂服务器跑服务端，本地网页链接，长对话时页面已经出现明显卡顿——而这甚至可能是酒馆框架本身的问题，跟AI无关。

换句话说，**即使你把AI放在了正确的位置上，上下文的膨胀仍然是悬在头顶的结构性成本**。纯AI驱动让这个问题放大了几十倍，但"正确架构"也没有消灭它，只是延缓了。

那RAG呢？检索增强生成——只塞相关内容，不塞全部历史——总该解决问题了吧？

我们试过。RAG方案在前期确实有效，但随着对话积累，检索本身需要处理的文档量也在膨胀。到后期，RAG模型的上下文窗口同样被撑爆了——不是因为它需要理解全部历史，而是因为它连判断"哪些历史是相关的"这件事，都需要越来越多的上下文。

**RAG延缓了崩溃，但没有改变崩溃的方向。**

### 过度设计陷阱

AI会给你设计`RequestQueueManager`类、`StateConsistencyValidator`接口、`AIResponseNormalizer`工厂——然后你会花三天实现这套架构，最后发现你的核心问题只需要三个函数。

AI生成的方案天然偏向教科书式的完整架构，而不是最小必要实现。纯AI驱动游戏的技术方案，几乎必然过度设计。

### 最终被SillyTavern教育

七天后，我们发现一个叫SillyTavern的开源项目，已经把我们想做的事情做完了，而且做得更好：
- 多模型支持
- 完整的角色卡系统
- 稳定的上下文管理
- 活跃的社区和插件生态

七天的工作，换来了一个教训：**动手之前先调研市面上有没有轮子。**

---

## 那AI在游戏里能做什么？

批评很容易，给出可行边界才有价值。经过实战验证，AI在游戏里有两个合理的位置：

### 位置一：纯叙事输出层

**AI只写文字，不碰数字。**

程序负责：掷骰子、计算伤害、判断成功失败、更新状态。
AI负责：把这些结果包装成好看的故事。

```
程序输出：攻击命中，伤害15，目标HP剩余8
AI输入：以上数据 + 角色背景 + 场景描述
AI输出：「匕首划过铠甲的缝隙，牧师踉跄后退，手压在伤口上，血从指缝渗出。」
```

AI不知道规则，只知道结果。它的工作是让"15点伤害"变成有温度的叙事，而不是计算出15这个数字。

### 位置二：辅助内容生成（离线）

地图描述、NPC背景故事、世界设定、物品描述——这些内容不影响实时游戏逻辑，可以预先生成、人工审核、存入数据库。

游戏运行时调用的不是AI，而是AI生成过的内容。

---

## 给想做AI游戏的人

如果你想用AI做游戏，建议在动手之前问自己三个问题：

**1. 这个功能需要确定性吗？**
需要确定性（战斗、经济、状态管理）→ 用代码。不需要（叙事、描述、风格化）→ 可以用AI。

**2. 这个功能需要精确数值吗？**
需要精确值 → AI不可信，用代码算完再交给AI描述。
不需要精确值 → AI可以参与。

**3. 市面上有没有现成方案？**
先花一天调研，再决定要不要自己做。七天造一个比SillyTavern差的轮子，是有成本的。

---

## 一句话

纯AI驱动游戏，是概率分布假装确定性系统、叙事一致性假装数值精确性、无限扩张的上下文假装真正的状态管理。

**AI是一个非常好的叙事引擎，一个非常糟糕的游戏引擎。把它放在对的位置上，它很强。**

---

*本文作者在七天内写了13个JavaScript模块，最终删掉了所有把数值计算交给AI的部分。SillyTavern感谢你的贡献。*

*下一篇：即使做了正确的架构，"把更多交给AI"的冲动本身为什么危险。*
